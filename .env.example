# Generated locally. Copy to `.env.local` only if you want to set these yourself.
# The app writes a `.env.local` with DATABASE_URL on first run if the file is missing.
# Never commit `.env.local` or real API keys.

DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/sde_tracker

# Optional — leave blank. Configure later in Settings or here.
OBSIDIAN_VAULT=
OBSIDIAN_TRACKER_DIR=Notion/tracker
LEETCODE_USERNAME=
LEETCODE_SESSION=
LEETCODE_CSRF=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
